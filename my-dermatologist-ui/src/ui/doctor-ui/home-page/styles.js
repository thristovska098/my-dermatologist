// @flow
import * as React from 'react';
import styled from 'styled-components';

export const ImageContainer: React.ComponentType<*> = styled.img`
  width: 290px;
`;

export const ImageContainerForModal: React.ComponentType<*> = styled.img`
  width: 100%;
`;
